import styled from 'styled-components';

const Title = styled.span`
  font-weight: 500;
  letter-spacing: .005em;
  line-height: 24px;
  font-size: 16px;
  max-height: 40px;
`;

export default Title;
